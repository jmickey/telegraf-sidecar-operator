version: "2"
run:
  allow-parallel-runners: true
linters:
  default: none
  enable:
    - copyloopvar
    - dupl
    - errcheck
    - goconst
    - gocyclo
    - govet
    - ineffassign
    - lll
    - misspell
    - nakedret
    - prealloc
    - staticcheck
    - unconvert
    - unparam
    - unused
  settings:
    govet:
      enable:
        - fieldalignment
  exclusions:
    warn-unused: true
    generated: lax
    rules:
      - linters:
          - dupl
          - lll
        path: internal/*
formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    generated: lax
